export enum NavItem {
  HOME = "Home",
  POPULAR = "Popular",
  TOP_RATED = "Top Rated",
  FAVORITES = "Favorites"
}

export enum NavItemPath {
  HOME = "/",
  POPULAR = "/popular",
  TOP_RATED = "/top-rated",
  FAVORITES = "/favorites"
}
