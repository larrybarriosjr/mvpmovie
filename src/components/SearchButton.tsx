const SearchButton = () => {
  return (
    <button
      type="submit"
      className="py-2 font-bold text-black border-2 rounded-full outline-none w-60 bg-primary border-primary hover:border-white"
    >
      Search
    </button>
  )
}

export default SearchButton
